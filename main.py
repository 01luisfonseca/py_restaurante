from tkinter import *

root = Tk()
root.geometry("1200x700+50+50")
# root.resizable(False, False)
root.title("PY Restaurante")

# # Fondo de azul suave
# root.config(bg="#000080")

# # Crear un frame para el menu
# menu_frame = Frame(root, bg="white")
# menu_frame.pack(side=LEFT, fill=Y)

# # Crear un frame para el contenido
# content_frame = Frame(root, bg="white")
# content_frame.pack(side=RIGHT, fill=BOTH, expand=True)

root.mainloop()
